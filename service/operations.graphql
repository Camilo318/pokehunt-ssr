query getPokemons($limit: Int, $page: Int) {
  pokemon(limit: $limit, page: $page) {
    id
    name
    hp
    attack
    defense
    image {
      id
    }
  }
  pokemon_aggregated {
    count {
      id
    }
  }
}
