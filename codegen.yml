overwrite: true
schema: '${NEXT_PUBLIC_POKEMON_URL}'
documents: 'service/operations.graphql'
generates:
  service/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typed-document-node'
